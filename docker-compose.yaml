version: "3"
services:
  app:
    container_name: cenote-api
    restart: always
    build: .
    ports:
      - "3000:3000"
    links:
      - mongo
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=mongodb://mongo:27017/cenote-db
      - PORT=3000
      - HOST=0.0.0.0
      - GLOBAL_LIMIT=5000
      - KAFKA_SERVERS=83.212.104.172:9092,83.212.96.15:9092,83.212.104.177:9092
      - KAFKA_TOPIC=cenoteIncoming
      - COCKROACH_URL=83.212.104.172
      - COCKROACH_PORT=30068
      - COCKROACH_DBNAME=cenote
      - COOKIE_SECRET=cenote-secret
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - "27017:27017"
